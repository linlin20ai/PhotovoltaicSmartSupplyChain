spring:
  datasource:
    url: jdbc:mysql://localhost:3306/pssc?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver
    druid:
      initial-size: 5 #????????
      min-idle: 10 #???????
      max-active: 20 #?????
      web-stat-filter:
        exclusions: "*.js,*.gif,*.jpg,*.png,*.css,*.ico,/druid/*" #?????????
      stat-view-servlet: #???????????????
        login-username: druid
        login-password: druid
  redis:
    host: localhost # Redis?????
    database: 0 # Redis?????????0?
    port: 6379 # Redis???????
    password: # Redis?????????????
    timeout: 300ms
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
server:
  port: 8080
# 修改swagger的路径匹配配置，使其兼容新版的SpringBoot
